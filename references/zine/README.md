# Moved

The zine studio moved to [`apps/zine`](../../apps/zine) — its own local-only git repo (no
GitHub remote, no CI; see `Roadmap/06-print-edition/zine-editing-central/README.md` Story 1.1).
This folder is kept only as a pointer so nothing links to a dead path.
