{
  "_about": "Every deny/ask rule in .claude/settings.json, with the rule it enforces and a probe command. Checked by scripts/permissions-smoke.mjs — a rule without an entry, or an entry without its rule, fails CI. Deny rules match the command text an agent normally writes; they are not a sandbox (see the script header).",
  "entries": [
    {
      "list": "deny",
      "rule": "Bash(vercel)",
      "probe": "vercel",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel .*)",
      "probe": "vercel .",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel deploy*)",
      "probe": "vercel deploy --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel deploy*)",
      "probe": "PATH=/x:$PATH vercel deploy --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel deploy*)",
      "probe": "env PATH=/x:$PATH vercel deploy --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel --prod*)",
      "probe": "vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel --prod*)",
      "probe": "PATH=/x:$PATH vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel --prod*)",
      "probe": "env PATH=/x:$PATH vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel --yes*)",
      "probe": "vercel --yes --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel --yes*)",
      "probe": "PATH=/x:$PATH vercel --yes --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel --yes*)",
      "probe": "env PATH=/x:$PATH vercel --yes --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel promote*)",
      "probe": "vercel promote dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel promote*)",
      "probe": "PATH=/x:$PATH vercel promote dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel promote*)",
      "probe": "env PATH=/x:$PATH vercel promote dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel rollback*)",
      "probe": "vercel rollback dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel rollback*)",
      "probe": "PATH=/x:$PATH vercel rollback dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel rollback*)",
      "probe": "env PATH=/x:$PATH vercel rollback dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel redeploy*)",
      "probe": "vercel redeploy dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel redeploy*)",
      "probe": "PATH=/x:$PATH vercel redeploy dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel redeploy*)",
      "probe": "env PATH=/x:$PATH vercel redeploy dpl_x",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel alias set*)",
      "probe": "vercel alias set dpl_x example.com",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel alias set*)",
      "probe": "PATH=/x:$PATH vercel alias set dpl_x example.com",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel alias set*)",
      "probe": "env PATH=/x:$PATH vercel alias set dpl_x example.com",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel rm *)",
      "probe": "vercel rm my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel rm *)",
      "probe": "PATH=/x:$PATH vercel rm my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel rm *)",
      "probe": "env PATH=/x:$PATH vercel rm my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(vercel remove *)",
      "probe": "vercel remove my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* vercel remove *)",
      "probe": "PATH=/x:$PATH vercel remove my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *vercel remove *)",
      "probe": "env PATH=/x:$PATH vercel remove my-project --yes",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx vercel*)",
      "probe": "npx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx vercel*)",
      "probe": "PATH=/x:$PATH npx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx vercel*)",
      "probe": "env PATH=/x:$PATH npx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx -y vercel*)",
      "probe": "npx -y vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx -y vercel*)",
      "probe": "PATH=/x:$PATH npx -y vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx -y vercel*)",
      "probe": "env PATH=/x:$PATH npx -y vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx --yes vercel*)",
      "probe": "npx --yes vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx --yes vercel*)",
      "probe": "PATH=/x:$PATH npx --yes vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx --yes vercel*)",
      "probe": "env PATH=/x:$PATH npx --yes vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(pnpm dlx vercel*)",
      "probe": "pnpm dlx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* pnpm dlx vercel*)",
      "probe": "PATH=/x:$PATH pnpm dlx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *pnpm dlx vercel*)",
      "probe": "env PATH=/x:$PATH pnpm dlx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(bunx vercel*)",
      "probe": "bunx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* bunx vercel*)",
      "probe": "PATH=/x:$PATH bunx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *bunx vercel*)",
      "probe": "env PATH=/x:$PATH bunx vercel --prod",
      "cites": "WAYS-OF-WORKING → The cadence (merging to `main` is the production deploy) — a CLI deploy is an out-of-band production release (a consuming project recorded an agent running `vercel --prod --yes`). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(supabase db push*)",
      "probe": "supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* supabase db push*)",
      "probe": "PATH=/x:$PATH supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *supabase db push*)",
      "probe": "env PATH=/x:$PATH supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(supabase * db push*)",
      "probe": "supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* supabase * db push*)",
      "probe": "PATH=/x:$PATH supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *supabase * db push*)",
      "probe": "env PATH=/x:$PATH supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(supabase db reset*)",
      "probe": "supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* supabase db reset*)",
      "probe": "PATH=/x:$PATH supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *supabase db reset*)",
      "probe": "env PATH=/x:$PATH supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(supabase * db reset*)",
      "probe": "supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* supabase * db reset*)",
      "probe": "PATH=/x:$PATH supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *supabase * db reset*)",
      "probe": "env PATH=/x:$PATH supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx supabase db push*)",
      "probe": "npx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx supabase db push*)",
      "probe": "PATH=/x:$PATH npx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx supabase db push*)",
      "probe": "env PATH=/x:$PATH npx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx supabase * db push*)",
      "probe": "npx supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx supabase * db push*)",
      "probe": "PATH=/x:$PATH npx supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx supabase * db push*)",
      "probe": "env PATH=/x:$PATH npx supabase --debug db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx supabase db reset*)",
      "probe": "npx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx supabase db reset*)",
      "probe": "PATH=/x:$PATH npx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx supabase db reset*)",
      "probe": "env PATH=/x:$PATH npx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx supabase * db reset*)",
      "probe": "npx supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx supabase * db reset*)",
      "probe": "PATH=/x:$PATH npx supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx supabase * db reset*)",
      "probe": "env PATH=/x:$PATH npx supabase --debug db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx -y supabase*)",
      "probe": "npx -y supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx -y supabase*)",
      "probe": "PATH=/x:$PATH npx -y supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx -y supabase*)",
      "probe": "env PATH=/x:$PATH npx -y supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(npx --yes supabase*)",
      "probe": "npx --yes supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* npx --yes supabase*)",
      "probe": "PATH=/x:$PATH npx --yes supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *npx --yes supabase*)",
      "probe": "env PATH=/x:$PATH npx --yes supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(pnpm dlx supabase*)",
      "probe": "pnpm dlx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* pnpm dlx supabase*)",
      "probe": "PATH=/x:$PATH pnpm dlx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *pnpm dlx supabase*)",
      "probe": "env PATH=/x:$PATH pnpm dlx supabase db push",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(bunx supabase*)",
      "probe": "bunx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* bunx supabase*)",
      "probe": "PATH=/x:$PATH bunx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *bunx supabase*)",
      "probe": "env PATH=/x:$PATH bunx supabase db reset",
      "cites": "WAYS-OF-WORKING → The cadence + Permissions → three actions get one focused question: a migration replay or a database reset against the LINKED project is irreversible against live data. The orchestrator applies migrations through the MCP; `db push` replays every local file and `db reset` drops the database. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push *--force)",
      "probe": "git push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push *--force)",
      "probe": "PATH=/x:$PATH git push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push *--force)",
      "probe": "env PATH=/x:$PATH git push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push *--force *)",
      "probe": "git push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push *--force *)",
      "probe": "PATH=/x:$PATH git push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push *--force *)",
      "probe": "env PATH=/x:$PATH git push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push -f*)",
      "probe": "git push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push -f*)",
      "probe": "PATH=/x:$PATH git push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push -f*)",
      "probe": "env PATH=/x:$PATH git push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * -f*)",
      "probe": "git push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * -f*)",
      "probe": "PATH=/x:$PATH git push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * -f*)",
      "probe": "env PATH=/x:$PATH git push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push -uf*)",
      "probe": "git push -uf origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push -uf*)",
      "probe": "PATH=/x:$PATH git push -uf origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push -uf*)",
      "probe": "env PATH=/x:$PATH git push -uf origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * -uf*)",
      "probe": "git push origin -uf main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * -uf*)",
      "probe": "PATH=/x:$PATH git push origin -uf main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * -uf*)",
      "probe": "env PATH=/x:$PATH git push origin -uf main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * +*)",
      "probe": "git push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * +*)",
      "probe": "PATH=/x:$PATH git push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * +*)",
      "probe": "env PATH=/x:$PATH git push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push *--mirror*)",
      "probe": "git push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push *--mirror*)",
      "probe": "PATH=/x:$PATH git push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push *--mirror*)",
      "probe": "env PATH=/x:$PATH git push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * :main)",
      "probe": "git push origin :main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * :main)",
      "probe": "PATH=/x:$PATH git push origin :main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * :main)",
      "probe": "env PATH=/x:$PATH git push origin :main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * :master)",
      "probe": "git push origin :master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * :master)",
      "probe": "PATH=/x:$PATH git push origin :master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * :master)",
      "probe": "env PATH=/x:$PATH git push origin :master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push *--delete *main)",
      "probe": "git push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push *--delete *main)",
      "probe": "PATH=/x:$PATH git push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push *--delete *main)",
      "probe": "env PATH=/x:$PATH git push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push *--delete *master)",
      "probe": "git push --delete origin master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push *--delete *master)",
      "probe": "PATH=/x:$PATH git push --delete origin master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push *--delete *master)",
      "probe": "env PATH=/x:$PATH git push --delete origin master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * -d *main)",
      "probe": "git push origin -d main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * -d *main)",
      "probe": "PATH=/x:$PATH git push origin -d main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * -d *main)",
      "probe": "env PATH=/x:$PATH git push origin -d main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git push * -d *master)",
      "probe": "git push origin -d master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git push * -d *master)",
      "probe": "PATH=/x:$PATH git push origin -d master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git push * -d *master)",
      "probe": "env PATH=/x:$PATH git push origin -d master",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push *--force)",
      "probe": "git -C /repo push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push *--force)",
      "probe": "PATH=/x:$PATH git -C /repo push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push *--force)",
      "probe": "env PATH=/x:$PATH git -C /repo push origin main --force",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push *--force *)",
      "probe": "git -C /repo push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push *--force *)",
      "probe": "PATH=/x:$PATH git -C /repo push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push *--force *)",
      "probe": "env PATH=/x:$PATH git -C /repo push --force origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push -f*)",
      "probe": "git -C /repo push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push -f*)",
      "probe": "PATH=/x:$PATH git -C /repo push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push -f*)",
      "probe": "env PATH=/x:$PATH git -C /repo push -f origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push * -f*)",
      "probe": "git -C /repo push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push * -f*)",
      "probe": "PATH=/x:$PATH git -C /repo push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push * -f*)",
      "probe": "env PATH=/x:$PATH git -C /repo push origin -f main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push *--mirror*)",
      "probe": "git -C /repo push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push *--mirror*)",
      "probe": "PATH=/x:$PATH git -C /repo push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push *--mirror*)",
      "probe": "env PATH=/x:$PATH git -C /repo push --mirror origin",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push * +*)",
      "probe": "git -C /repo push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push * +*)",
      "probe": "PATH=/x:$PATH git -C /repo push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push * +*)",
      "probe": "env PATH=/x:$PATH git -C /repo push origin +main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git -C * push *--delete *main)",
      "probe": "git -C /repo push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git -C * push *--delete *main)",
      "probe": "PATH=/x:$PATH git -C /repo push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git -C * push *--delete *main)",
      "probe": "env PATH=/x:$PATH git -C /repo push --delete origin main",
      "cites": "WAYS-OF-WORKING → Conventions → Gitflow: never force-push a shared branch. A force, a `+ref`, a `--mirror` and a branch DELETE all rewrite or remove history other agents are working on. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -rf*)",
      "probe": "rm -rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -rf*)",
      "probe": "PATH=/x:$PATH rm -rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -rf*)",
      "probe": "env PATH=/x:$PATH rm -rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -fr*)",
      "probe": "rm -fr build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -fr*)",
      "probe": "PATH=/x:$PATH rm -fr build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -fr*)",
      "probe": "env PATH=/x:$PATH rm -fr build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -Rf*)",
      "probe": "rm -Rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -Rf*)",
      "probe": "PATH=/x:$PATH rm -Rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -Rf*)",
      "probe": "env PATH=/x:$PATH rm -Rf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -fR*)",
      "probe": "rm -fR build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -fR*)",
      "probe": "PATH=/x:$PATH rm -fR build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -fR*)",
      "probe": "env PATH=/x:$PATH rm -fR build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -vrf*)",
      "probe": "rm -vrf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -vrf*)",
      "probe": "PATH=/x:$PATH rm -vrf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -vrf*)",
      "probe": "env PATH=/x:$PATH rm -vrf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -rvf*)",
      "probe": "rm -rvf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -rvf*)",
      "probe": "PATH=/x:$PATH rm -rvf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -rvf*)",
      "probe": "env PATH=/x:$PATH rm -rvf build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -rfv*)",
      "probe": "rm -rfv build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -rfv*)",
      "probe": "PATH=/x:$PATH rm -rfv build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -rfv*)",
      "probe": "env PATH=/x:$PATH rm -rfv build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -r -f*)",
      "probe": "rm -r -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -r -f*)",
      "probe": "PATH=/x:$PATH rm -r -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -r -f*)",
      "probe": "env PATH=/x:$PATH rm -r -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -R -f*)",
      "probe": "rm -R -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -R -f*)",
      "probe": "PATH=/x:$PATH rm -R -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -R -f*)",
      "probe": "env PATH=/x:$PATH rm -R -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -f -r*)",
      "probe": "rm -f -r build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -f -r*)",
      "probe": "PATH=/x:$PATH rm -f -r build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -f -r*)",
      "probe": "env PATH=/x:$PATH rm -f -r build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -f -R*)",
      "probe": "rm -f -R build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -f -R*)",
      "probe": "PATH=/x:$PATH rm -f -R build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -f -R*)",
      "probe": "env PATH=/x:$PATH rm -f -R build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -r --force*)",
      "probe": "rm -r --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -r --force*)",
      "probe": "PATH=/x:$PATH rm -r --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -r --force*)",
      "probe": "env PATH=/x:$PATH rm -r --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm -R --force*)",
      "probe": "rm -R --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm -R --force*)",
      "probe": "PATH=/x:$PATH rm -R --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm -R --force*)",
      "probe": "env PATH=/x:$PATH rm -R --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm --recursive --force*)",
      "probe": "rm --recursive --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm --recursive --force*)",
      "probe": "PATH=/x:$PATH rm --recursive --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm --recursive --force*)",
      "probe": "env PATH=/x:$PATH rm --recursive --force build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm --force --recursive*)",
      "probe": "rm --force --recursive build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm --force --recursive*)",
      "probe": "PATH=/x:$PATH rm --force --recursive build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm --force --recursive*)",
      "probe": "env PATH=/x:$PATH rm --force --recursive build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(rm --recursive -f*)",
      "probe": "rm --recursive -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* rm --recursive -f*)",
      "probe": "PATH=/x:$PATH rm --recursive -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *rm --recursive -f*)",
      "probe": "env PATH=/x:$PATH rm --recursive -f build",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: a destructive change is the one irreversible class. A consuming project's local approvals had accreted `rm -rf .git`. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add -A*)",
      "probe": "git add -A",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add -A*)",
      "probe": "PATH=/x:$PATH git add -A",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add -A*)",
      "probe": "env PATH=/x:$PATH git add -A",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add --all*)",
      "probe": "git add --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add --all*)",
      "probe": "PATH=/x:$PATH git add --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add --all*)",
      "probe": "env PATH=/x:$PATH git add --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add .)",
      "probe": "git add .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add .)",
      "probe": "PATH=/x:$PATH git add .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add .)",
      "probe": "env PATH=/x:$PATH git add .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add -- .)",
      "probe": "git add -- .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add -- .)",
      "probe": "PATH=/x:$PATH git add -- .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add -- .)",
      "probe": "env PATH=/x:$PATH git add -- .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add ./)",
      "probe": "git add ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add ./)",
      "probe": "PATH=/x:$PATH git add ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add ./)",
      "probe": "env PATH=/x:$PATH git add ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add -- ./*)",
      "probe": "git add -- ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add -- ./*)",
      "probe": "PATH=/x:$PATH git add -- ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add -- ./*)",
      "probe": "env PATH=/x:$PATH git add -- ./",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add -- :/*)",
      "probe": "git add -- :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add -- :/*)",
      "probe": "PATH=/x:$PATH git add -- :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add -- :/*)",
      "probe": "env PATH=/x:$PATH git add -- :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add * .)",
      "probe": "git add -v .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add * .)",
      "probe": "PATH=/x:$PATH git add -v .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add * .)",
      "probe": "env PATH=/x:$PATH git add -v .",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add :/*)",
      "probe": "git add :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add :/*)",
      "probe": "PATH=/x:$PATH git add :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add :/*)",
      "probe": "env PATH=/x:$PATH git add :/",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add -u)",
      "probe": "git add -u",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add -u)",
      "probe": "PATH=/x:$PATH git add -u",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add -u)",
      "probe": "env PATH=/x:$PATH git add -u",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git add --update)",
      "probe": "git add --update",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git add --update)",
      "probe": "PATH=/x:$PATH git add --update",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git add --update)",
      "probe": "env PATH=/x:$PATH git add --update",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit -a *)",
      "probe": "git commit -a -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit -a *)",
      "probe": "PATH=/x:$PATH git commit -a -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit -a *)",
      "probe": "env PATH=/x:$PATH git commit -a -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit -am *)",
      "probe": "git commit -am x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit -am *)",
      "probe": "PATH=/x:$PATH git commit -am x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit -am *)",
      "probe": "env PATH=/x:$PATH git commit -am x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit -qam *)",
      "probe": "git commit -qam x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit -qam *)",
      "probe": "PATH=/x:$PATH git commit -qam x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit -qam *)",
      "probe": "env PATH=/x:$PATH git commit -qam x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit -av*)",
      "probe": "git commit -av -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit -av*)",
      "probe": "PATH=/x:$PATH git commit -av -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit -av*)",
      "probe": "env PATH=/x:$PATH git commit -av -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit --all*)",
      "probe": "git commit --all -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit --all*)",
      "probe": "PATH=/x:$PATH git commit --all -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit --all*)",
      "probe": "env PATH=/x:$PATH git commit --all -m x",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit * -a)",
      "probe": "git commit -m x -a",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit * -a)",
      "probe": "PATH=/x:$PATH git commit -m x -a",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit * -a)",
      "probe": "env PATH=/x:$PATH git commit -m x -a",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(git commit * --all)",
      "probe": "git commit -m x --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* git commit * --all)",
      "probe": "PATH=/x:$PATH git commit -m x --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *git commit * --all)",
      "probe": "env PATH=/x:$PATH git commit -m x --all",
      "cites": "WAYS-OF-WORKING → Conventions → Path-limited commits: several agents share a checkout, so whole-tree staging commits a sibling's in-flight work. Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(node scripts/vercel-env.mjs set*)",
      "probe": "node scripts/vercel-env.mjs set STRIPE_SECRET_KEY sk_live_x --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* node scripts/vercel-env.mjs set*)",
      "probe": "PATH=/x:$PATH node scripts/vercel-env.mjs set STRIPE_SECRET_KEY sk_live_x --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *node scripts/vercel-env.mjs set*)",
      "probe": "env PATH=/x:$PATH node scripts/vercel-env.mjs set STRIPE_SECRET_KEY sk_live_x --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(node scripts/vercel-env.mjs delete*)",
      "probe": "node scripts/vercel-env.mjs delete DATABASE_URL --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* node scripts/vercel-env.mjs delete*)",
      "probe": "PATH=/x:$PATH node scripts/vercel-env.mjs delete DATABASE_URL --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *node scripts/vercel-env.mjs delete*)",
      "probe": "env PATH=/x:$PATH node scripts/vercel-env.mjs delete DATABASE_URL --env production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(node scripts/*../*)",
      "probe": "node scripts/../../../tmp/evil.mjs",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(*=* node scripts/*../*)",
      "probe": "PATH=/x:$PATH node scripts/../../../tmp/evil.mjs",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Bash(env *node scripts/*../*)",
      "probe": "env PATH=/x:$PATH node scripts/../../../tmp/evil.mjs",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: `Bash(node scripts/*)` is an ALLOW, and an allowed command skips the classifier — so the few repo scripts that write production secrets or escape the scripts directory are denied by name (found by the security lens on #17: the `ask` rules guarded the `vercel env` CLI path while the REST path these scripts use was pre-approved). Generated in all three prefix spellings (bare, assignment-prefixed, `env`-prefixed): a leading assignment whose value contains an expansion was observed LIVE 2026-09-16 to escape a bare rule. Deliberately NARROW rather than a `*=* <binary>*` catch-all, which also refused ordinary reads like `grep -rn --include=*.json vercel .` (found by the security lens on #17)."
    },
    {
      "list": "deny",
      "rule": "Edit(/Roadmap/00-ideas/BUILD-ORDER.md)",
      "cites": "AGENTS.md rule #2: generated artifacts are NEVER hand-edited — BUILD-ORDER.md and OWED-LEDGER.{md,json} are outputs; a hand edit is silently reverted by the next regeneration (CI guards staleness). (`Write(<path>)` was dropped 2026-09-16: Claude Code checks only `Edit(<path>)` for FILE tools — a nested session refuses to start while a Write rule is present, saying so in as many words. The Edit rule covers Write, Edit and NotebookEdit alike, so nothing is lost.)"
    },
    {
      "list": "deny",
      "rule": "Edit(/Roadmap/00-ideas/OWED-LEDGER.md)",
      "cites": "AGENTS.md rule #2: generated artifacts are NEVER hand-edited — BUILD-ORDER.md and OWED-LEDGER.{md,json} are outputs; a hand edit is silently reverted by the next regeneration (CI guards staleness). (`Write(<path>)` was dropped 2026-09-16: Claude Code checks only `Edit(<path>)` for FILE tools — a nested session refuses to start while a Write rule is present, saying so in as many words. The Edit rule covers Write, Edit and NotebookEdit alike, so nothing is lost.)"
    },
    {
      "list": "deny",
      "rule": "Edit(/Roadmap/00-ideas/OWED-LEDGER.json)",
      "cites": "AGENTS.md rule #2: generated artifacts are NEVER hand-edited — BUILD-ORDER.md and OWED-LEDGER.{md,json} are outputs; a hand edit is silently reverted by the next regeneration (CI guards staleness). (`Write(<path>)` was dropped 2026-09-16: Claude Code checks only `Edit(<path>)` for FILE tools — a nested session refuses to start while a Write rule is present, saying so in as many words. The Edit rule covers Write, Edit and NotebookEdit alike, so nothing is lost.)"
    },
    {
      "list": "ask",
      "rule": "Bash(vercel env add *)",
      "probe": "vercel env add FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* vercel env add *)",
      "probe": "PATH=/x:$PATH vercel env add FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *vercel env add *)",
      "probe": "env PATH=/x:$PATH vercel env add FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(vercel env rm *)",
      "probe": "vercel env rm FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* vercel env rm *)",
      "probe": "PATH=/x:$PATH vercel env rm FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *vercel env rm *)",
      "probe": "env PATH=/x:$PATH vercel env rm FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(vercel env update *)",
      "probe": "vercel env update FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* vercel env update *)",
      "probe": "PATH=/x:$PATH vercel env update FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *vercel env update *)",
      "probe": "env PATH=/x:$PATH vercel env update FOO production",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gh secret set *)",
      "probe": "gh secret set STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gh secret set *)",
      "probe": "PATH=/x:$PATH gh secret set STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gh secret set *)",
      "probe": "env PATH=/x:$PATH gh secret set STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gh secret delete *)",
      "probe": "gh secret delete STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gh secret delete *)",
      "probe": "PATH=/x:$PATH gh secret delete STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gh secret delete *)",
      "probe": "env PATH=/x:$PATH gh secret delete STRIPE_SECRET_KEY",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud secrets create *)",
      "probe": "gcloud secrets create api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud secrets create *)",
      "probe": "PATH=/x:$PATH gcloud secrets create api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud secrets create *)",
      "probe": "env PATH=/x:$PATH gcloud secrets create api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud secrets delete *)",
      "probe": "gcloud secrets delete api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud secrets delete *)",
      "probe": "PATH=/x:$PATH gcloud secrets delete api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud secrets delete *)",
      "probe": "env PATH=/x:$PATH gcloud secrets delete api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud secrets versions add *)",
      "probe": "gcloud secrets versions add api-key --data-file=-",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud secrets versions add *)",
      "probe": "PATH=/x:$PATH gcloud secrets versions add api-key --data-file=-",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud secrets versions add *)",
      "probe": "env PATH=/x:$PATH gcloud secrets versions add api-key --data-file=-",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud secrets versions destroy *)",
      "probe": "gcloud secrets versions destroy 1 --secret=api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud secrets versions destroy *)",
      "probe": "PATH=/x:$PATH gcloud secrets versions destroy 1 --secret=api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud secrets versions destroy *)",
      "probe": "env PATH=/x:$PATH gcloud secrets versions destroy 1 --secret=api-key",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud run services update *--set-env-vars*)",
      "probe": "gcloud run services update svc --set-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud run services update *--set-env-vars*)",
      "probe": "PATH=/x:$PATH gcloud run services update svc --set-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud run services update *--set-env-vars*)",
      "probe": "env PATH=/x:$PATH gcloud run services update svc --set-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud run services update *--update-env-vars*)",
      "probe": "gcloud run services update svc --update-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud run services update *--update-env-vars*)",
      "probe": "PATH=/x:$PATH gcloud run services update svc --update-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud run services update *--update-env-vars*)",
      "probe": "env PATH=/x:$PATH gcloud run services update svc --update-env-vars A=b",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud run services update *--set-secrets*)",
      "probe": "gcloud run services update svc --set-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud run services update *--set-secrets*)",
      "probe": "PATH=/x:$PATH gcloud run services update svc --set-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud run services update *--set-secrets*)",
      "probe": "env PATH=/x:$PATH gcloud run services update svc --set-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud run services update *--update-secrets*)",
      "probe": "gcloud run services update svc --update-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud run services update *--update-secrets*)",
      "probe": "PATH=/x:$PATH gcloud run services update svc --update-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud run services update *--update-secrets*)",
      "probe": "env PATH=/x:$PATH gcloud run services update svc --update-secrets A=s:latest",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(git push *--force-with-lease*)",
      "probe": "git push --force-with-lease origin main",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* git push *--force-with-lease*)",
      "probe": "PATH=/x:$PATH git push --force-with-lease origin main",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *git push *--force-with-lease*)",
      "probe": "env PATH=/x:$PATH git push --force-with-lease origin main",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(gcloud run deploy *)",
      "probe": "gcloud run deploy svc --image x",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(*=* gcloud run deploy *)",
      "probe": "PATH=/x:$PATH gcloud run deploy svc --image x",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    },
    {
      "list": "ask",
      "rule": "Bash(env *gcloud run deploy *)",
      "probe": "env PATH=/x:$PATH gcloud run deploy svc --image x",
      "cites": "WAYS-OF-WORKING → Permissions → three actions get one focused question: production secrets/IAM and a deploy of a running service. ASK, not deny — secret names are not enumerable, so a name-match rule would be theatre; the question is the control. Carried into the assignment- and `env`-prefixed spellings too: an ask rule escapes the same way a deny rule does, and an escaped ASK silently becomes a classifier judgement instead of a human question (found by the security lens on #17)."
    }
  ]
}
