# 09 · Platform & Infra

> **What this is.** The home for **infra / tooling / observability** epics that keep the platform
> healthy but don't sit inside a buyer/seller/agent product journey (macro-sections 01–08). The
> product poster (`Roadmap/README.md`) stays product-only; this area holds the engineering-facing
> work that has its own scope, slices, and Definition of Done.

Status legend: ✅ Live · 🚧 In progress · 📋 Planned

## Epics

| Epic | In one line | Status |
|---|---|---|
| [backend-production-readiness](backend-production-readiness/) | Backend prod-readiness audit (staging · backups · recovery · monitoring · security · scaling) → hardening; opens with an audit spike that decides the staging platform | ✅ **COMPLETE — all 5 sprints shipped 2026-06-12** — S0 audit · S1 staging (`medusa-web-staging` + Neon branch) · S2 backups (escrow LIVE + both restore drills) · S3 recovery (HTTP `/health` probes on prod + rollback runbook) · S4 monitoring (uptime + Cloud Run alert policies → `MiyagiDevopsTele` + Error Reporting + Dependabot + `deploy.sh` drift guard; cross-reviewed, **provisioned LIVE on prod**; S4 squash root #13 `d50d0b4` + backend #21 `8be6ab6`). Residual: Daniel's Telegram-receipt eyeball |
| [cicd-telegram-notifications](cicd-telegram-notifications/) | Unified CI/CD + git-event notifications into a dedicated Telegram channel (push + prod-deploy finish, both repos) | ✅ Shipped 2026-06-06 (all 3 sprints; S1 push pings both repos · S2 backend Pub/Sub deploy-finish `76c8639` · S3 Vercel-prod poll). Live channel setup + push/deploy-finish confirmation owed to Daniel (operational) |
| [design-token-foundation](design-token-foundation/) | Harden + document the existing design-token foundation (token contract, locked/unlockable matrix, tokenize customer-facing surfaces, AA contrast + no-regression guard) — substrate for #6 | ✅ Shipped 2026-06-07 (PR #37; closed out — `token-contract.md` written, retro done; screenshot-diff owed to Daniel) |
| [feature-flags-killswitches](feature-flags-killswitches/) | Flagsmith as the platform's fail-open, admin-only, server-evaluated kill-switch layer — `checkout.stripe_enabled` enforced across UI + agents/UCP + checkout | ✅ Shipped 2026-06-06 (S1 PR #34 + S2 PR #9); further taxonomy deferred by decision |
| [navigation-settings-reorg](navigation-settings-reorg/) | Separate buyer / seller / agent chrome that had been jammed into one nav — PWA bottom bar, header, settings reorg, global app-shell suppression gate (S3 HIGH) | ✅ Shipped 2026-06-11 (all 4 sprints; PRs #75/#77/#80/#81; frontend-only) |
| [cross-agent-code-review](cross-agent-code-review/) | Advisory cross-agent second-opinion PR review (`scripts/cross-review.mjs` → Codex/Antigravity), single-pass, never gates | ✅ Shipped 2026-06-10 (1 sprint; first real run on PR #8 backend-staging) |
| [cross-agent-planning-panel](cross-agent-planning-panel/) | The planning analog of cross-agent review (`scripts/cross-panel.mjs`) — single-pass advisory architecture lenses (purist/pragmatist) on a scope doc, surfaced by `groom` on spikes/architecture forks, never gates | ✅ **Shipped 2026-06-13** (1 sprint, all LOW; PR #15) — two lenses + pair-run synthesis; shared CLI plumbing in `scripts/lib/cross-agent-cli.mjs` |
| [dev-tooling-reliability](dev-tooling-reliability/) | Three ship-loop frictions fixed as scripts/config: backend pre-merge CI, Codex→Antigravity endurance, and the cross-review wrong-branch tax | ✅ **COMPLETE — all 3 sprints shipped 2026-06-21** — S1 backend `ci.yml` + required check (backend #29 `21b1e16`) · S2 Codex→Antigravity auto-fallback (#16 `cbed936`) · S3 PR# optional + resolve-from-branch + stale-HEAD guard (#17 `2534aeb`). All LOW; `node:test` is the CLI gate |
| [neon-egress-and-db-isolation](neon-egress-and-db-isolation/) | Execute the approved Neon-egress spike: cache storefront reads, quiet the always-on backend (`minScale` + job cadence), split side-projects off the commerce Neon org, install site-wide Clarity, housekeeping | ⚠️ **Archived/superseded 2026-06-22** — the egress root cause was solved architecturally by [postgres-neon-to-cloudsql](postgres-neon-to-cloudsql/) (co-locate the DB on Cloud SQL); its orphaned **S4** (site-wide Clarity loader + UTM) is **superseded by [site-wide-analytics-gtm](site-wide-analytics-gtm/)** |
| [cross-agent-review-always](cross-agent-review-always/) | Make the cross-agent second opinion happen on **every** PR (not just HIGH-risk) — evaluated CI auto-run vs local | ✅ **Shipped 2026-06-22 (local-only, by decision)** — CI auto-run **descoped** (a runner has no codex/agy auth: codex needs a billed API key + cross-repo PAT, agy has none — not worth it for an advisory aid). Shipped: `--skip-trivial` cost guard (root #29) + policy flip (WAYS / PR template / kickoffs → "run **locally** on every PR, advisory"). Workflow built + self-smoked green but unmerged (app PRs `#103`/`#35` closed) |
| [admin-consolidation](admin-consolidation/) | Consolidate the scattered admin into one in-repo shell + nav, move human auth from shared `ADMIN_SECRET`-in-URL to Clerk identity + audit, and add tenant (shop) management (directory + entitlement grant) | 📋 **Scaffolded 2026-06-22** (4 sprints, from the approved admin-consolidation spike) — S1 shell + Clerk hub (dual-accept) · S2 audit + migrate sections + kill secret-in-URL (HIGH) · S3 tenant directory (read-only) · S4 entitlement grant/revoke (HIGH). Suspend deferred to its own Medusa-first epic; per-shop flags dropped |
| [site-wide-analytics-gtm](site-wide-analytics-gtm/) | Load a single GTM container site-wide (GA4 + Microsoft Clarity as tags inside GTM), client-gated to platform surfaces and static-safe so it doesn't undo the static `(site)` shell | ✅ **Shipped 2026-06-22** (1 sprint, all LOW, frontend-only; PR #106) — pure `lib/analytics-gating.ts` gate + `<SiteAnalytics>` loader in the static root layout (`/` stays `○`); `NEXT_PUBLIC_GTM_ID=GTM-MWHVLJ3M` set in Vercel. Owed to Daniel (operational, no code): activate Clarity via its 1-click GTM wizard (was 1 session/30d), optional GA4 (needs a GA4 property), firing smoke. Folds in the orphaned neon-egress S4 |
